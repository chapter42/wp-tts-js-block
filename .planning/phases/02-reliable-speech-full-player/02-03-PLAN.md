---
phase: 02-reliable-speech-full-player
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [PLAY-07, PLAY-08]

must_haves:
  truths:
    - "Plugin builds without errors after Plan 01 and 02 changes"
    - "All Phase 2 DOM elements are present in build output"
    - "Player visually correct on desktop and mobile"
  artifacts: []
  key_links: []
---

<objective>
Build the plugin and verify all Phase 2 changes work together. Human verifies visual layout on desktop and mobile.

Purpose: Catch build errors, missing elements, or visual regressions before considering Phase 2 complete. Ensures markup from Plan 01 and JS from Plan 02 integrate correctly.

Output: Verified build, visual confirmation of player layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-reliable-speech-full-player/02-01-SUMMARY.md
@.planning/phases/02-reliable-speech-full-player/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build plugin and verify integration</name>
  <files></files>
  <read_first>
    - tts-js/src/tts-js/render.php (verify Plan 01 changes present)
    - tts-js/src/tts-js/view.js (verify Plan 02 changes present)
    - tts-js/src/tts-js/style.scss (verify Plan 01 changes present)
    - tts-js/src/tts-js/edit.js (verify Plan 01 changes present)
  </read_first>
  <action>
1. Run `cd tts-js && npm run build` — must complete with exit code 0.

2. Verify build output contains all Phase 2 elements:
   - `grep "tts-progress" tts-js/build/tts-js/render.php` — must match (progress bar in markup)
   - `grep "tts-speed-btn" tts-js/build/tts-js/render.php` — must match (speed button in markup)
   - `grep "data-tts-words" tts-js/build/tts-js/render.php` — must match (word count attribute)
   - `grep "tts-icon--error" tts-js/build/tts-js/render.php` — must match (error icon)
   - Check that build/tts-js/view.js exists and contains `splitIntoChunks` or minified equivalent (search for distinctive strings like `SPEED_STEPS` or `resolveVoice` or `playNextChunk`)
   - Check that build/tts-js/style-index.css exists and contains `tts-progress` and `tts-speed-btn`

3. Verify DOM element class names in render.php match the querySelector strings in view.js:
   - view.js queries `.tts-speed-btn` — render.php must have `class="tts-speed-btn"`
   - view.js queries `.tts-progress` — render.php must have `class="tts-progress"`
   - view.js queries `.tts-progress__fill` — render.php must have `class="tts-progress__fill"`
   - view.js queries `.tts-duration` — render.php must have `class="tts-duration"`

4. If build fails, check error output and fix the issue in the relevant source file.
  </action>
  <verify>
    <automated>cd "/Users/royhuiskes/Claude Code/tts-js/tts-js" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - build/tts-js/render.php contains "tts-progress", "tts-speed-btn", "data-tts-words", "tts-icon--error"
    - build/tts-js/view.js exists and is non-empty
    - build/tts-js/style-index.css contains "tts-progress" and "tts-speed-btn"
    - build/tts-js/index.js exists (editor script)
    - No class name mismatches between render.php and view.js
  </acceptance_criteria>
  <done>Plugin builds cleanly. All Phase 2 elements present in build output. DOM class names consistent between markup and JS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 2 player</name>
  <action>
Present the human with verification steps for the complete Phase 2 player. The human needs to test the plugin in a real WordPress environment.
  </action>
  <what-built>Complete Phase 2 player: chunked speech engine, voice auto-selection, speed cycling (0.8x-1.5x), progress bar, duration display with remaining time, mobile responsive layout, error state for missing voices.</what-built>
  <how-to-verify>
    1. Copy the tts-js/ folder to your WordPress plugins directory and activate the plugin
    2. Open a post with the TTS block in the editor:
       - Verify the preview shows: play button, label, duration ("~3 min"), progress bar at ~40%, speed button ("1x"), stop button
       - Verify the sidebar speed setting shows a dropdown with 8 options (0.8x through 1.5x)
    3. View the published post on desktop:
       - Click play — verify speech starts reading the article
       - Verify progress bar advances at each sentence boundary
       - Verify duration changes to "~N min resterend" during playback
       - Click the speed button — verify it cycles through 0.8x, 0.9x, 1x, etc.
       - Speed change should take effect at the next sentence
       - Click pause — verify current sentence finishes then pauses
       - Click play again — verify it resumes from where it paused
       - Click stop — verify it resets (progress bar gone, duration back to estimate)
       - Let a full article play to completion — verify it does NOT cut off at 15 seconds
    4. View the published post on mobile (or use browser dev tools at 480px and 360px):
       - Verify player layout looks good: no overflow, buttons accessible, text readable
       - Verify speed button is visible and tappable
    5. (Optional) Test in a browser with no Dutch voice available:
       - Verify player shows "Geen geschikte stem gevonden" and "Probeer een andere browser"
  </how-to-verify>
  <verify>Human approves the visual and functional result</verify>
  <done>Human confirms: speech plays full article without cutoff, speed cycling works, progress bar tracks, duration shows remaining, mobile layout correct</done>
  <resume-signal>Type "approved" if everything works, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
Build succeeds. Human confirms player works end-to-end on desktop and mobile.
</verification>

<success_criteria>
- Plugin builds without errors
- Speech plays a full article without 15s cutoff
- Speed cycling works
- Progress bar tracks chunk position
- Duration shows remaining time during playback
- Player looks correct on desktop and mobile
- Human approves the visual and functional result
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliable-speech-full-player/02-03-SUMMARY.md`
</output>
